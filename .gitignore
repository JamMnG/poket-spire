# ── 그림 자산 ────────────────────────────────────────────────
# 닌텐도·게임프리크의 저작물이라 저장소에 올리지 않는다.
# 필요하면 `py fetch_assets.py` 한 번으로 받아 온다.
# 안 받아도 게임은 돌아간다 — 코드로 찍은 도트가 대체 그림으로 들어간다.
src/assets/pokemon/
src/assets/items/

# ── 그 밖에 ──────────────────────────────────────────────────
__pycache__/
*.pyc
.DS_Store
Thumbs.db
*.log
out.txt
