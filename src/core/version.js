// ─────────────────────────────────────────────────────────────
// version.js — 화면에 찍히는 빌드 표시
//
// 왜 있나: "고쳤다는데 그대로다" 라는 말이 나올 때마다, 배포가 안 된 것인지
// 브라우저가 옛 파일을 붙들고 있는 것인지 진짜 안 고쳐진 것인지를 가릴
// 방법이 없었다. 이 게임은 빌드 과정이 없어서 파일 이름에 해시가 안 붙고,
// 그래서 옛 모듈이 캐시에 남으면 겉으로는 티가 안 난다.
//
// 타이틀 오른쪽 아래와 상단 정보띠에 이 값을 찍는다. 화면의 값과 아래
// 목록이 다르면 그건 캐시다 — 강력 새로고침(Ctrl+F5) 으로 풀린다.
//
// ★ 규칙: 게임 규칙이나 밸런스를 건드린 커밋에서는 이 값을 같이 올린다.
// ─────────────────────────────────────────────────────────────

export const BUILD = 'v10';

/** 이 빌드에서 무엇이 바뀌었는지 — 타이틀 툴팁에 뜬다 */
export const BUILD_NOTE = '엘리트가 진짜 위험해짐 · 보스가 엘리트보다 세짐';
